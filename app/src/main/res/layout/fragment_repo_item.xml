<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="8dp"
    android:layout_marginTop="4dp"
    android:layout_marginEnd="8dp"
    android:layout_marginBottom="4dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Columna Izquierda: Imagen y Botones -->
        <ImageView
            android:id="@+id/repo_owner_image"
            android:layout_width="64dp"
            android:layout_height="64dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:srcCompat="@tools:sample/avatars" />

        <ImageButton
            android:id="@+id/edit_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Editar repositorio"
            android:padding="4dp"
            android:src="@drawable/ic_edit"
            app:tint="?attr/colorPrimary"
            app:layout_constraintTop_toBottomOf="@id/repo_owner_image"
            app:layout_constraintStart_toStartOf="@id/repo_owner_image"
            app:layout_constraintEnd_toStartOf="@id/delete_button"
            app:layout_constraintHorizontal_chainStyle="packed" />

        <ImageButton
            android:id="@+id/delete_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Eliminar repositorio"
            android:padding="4dp"
            android:src="@drawable/ic_delete"
            app:tint="@android:color/holo_red_dark"
            app:layout_constraintTop_toTopOf="@id/edit_button"
            app:layout_constraintStart_toEndOf="@id/edit_button"
            app:layout_constraintEnd_toEndOf="@id/repo_owner_image" />

        <!-- Columna Derecha: Texto -->
        <TextView
            android:id="@+id/repo_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:textStyle="bold"
            android:textSize="16sp"
            app:layout_constraintStart_toEndOf="@id/repo_owner_image"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="nombre-del-repositorio-ejemplo" />

        <TextView
            android:id="@+id/repo_description"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            app:layout_constraintStart_toStartOf="@id/repo_name"
            app:layout_constraintEnd_toEndOf="@id/repo_name"
            app:layout_constraintTop_toBottomOf="@id/repo_name"
            tools:text="Esta es una descripción larga de un repositorio de ejemplo para ver cómo se ajusta el texto." />

        <TextView
            android:id="@+id/repo_lang"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textStyle="italic"
            app:layout_constraintStart_toStartOf="@id/repo_name"
            app:layout_constraintEnd_toEndOf="@id/repo_name"
            app:layout_constraintTop_toBottomOf="@id/repo_description"
            tools:text="Kotlin" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>